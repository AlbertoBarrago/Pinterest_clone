.row
  .col-md-12
    #pins.transitions-enabled.centered.clearfix.masonry
      - @pins.each do |pin|
        .box.panel.panel-default
          = link_to (image_tag pin.image.url), pin
          .panel-body
            %h2= link_to pin.title, pin
            %p.user
              Inviato da
              = pin.user.email


              .fb-like
                data-share="true"
                data-width="450"
                data-show-faces="true">
